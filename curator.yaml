elasticsearch:
  client:
    hosts:
      - http://ELASTICSEARCH:PORT
    request_timeout: 30
  other_settings:
    master_only: False
    username: USER
    password: PASS

logging:
  loglevel: INFO
  logfile: ''
  logformat: default
  blacklist: ['elasticsearch', 'urllib3']
